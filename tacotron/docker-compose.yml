version: '2'
services:
    forward_tacotron:
        build: .
        ports:
            - 9999:9999
            - 6006:6006
        volumes:
            - ../../datasets:/root/ForwardTacotron/datasets
        environment:
            - PASSWORD=secret
        tty: true
